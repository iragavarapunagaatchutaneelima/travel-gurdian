# Travel Guardian — Master Issue Inventory & Resolution Tracker

This document catalogs all identified architectural, navigation, AI, location, UI/UX, and performance issues across the Travel Guardian platform, in accordance with the Phase Analysis and Quality Assurance standard.

| ID | Category | Issue | Root Cause | Severity | Status | Solution & Verification |
|----|----------|-------|------------|----------|--------|-------------------------|
| **ISS-01** | G. GOOGLE MAPS / E. UI/UX | Navigation HUD bottom telemetry overlay rendered as an opaque white box that looked detached from the living map canvas. | `LiveNavigationOverlay.tsx` used hardcoded `#FFFFFF` / `#F8FAFC` backgrounds rather than translucent dark/blue glassmorphism overlay with backdrop blur, rounded corners, and compact responsive metrics. | High | **FIXED** | Redesigned `LiveNavigationOverlay.tsx` to use a translucent dark glassmorphism HUD (`rgba(15, 23, 42, 0.85)` with `backdrop-blur-xl`, `border-white/15`, rounded-3xl container, and 4 compact metric cards: ETA, SPEED, HEADING, SAFETY). |
| **ISS-02** | E. UI/UX / L. DESKTOP | "Welcome Traveler" dashboard hero card background completely obscured the `/hero1.png` scenic image. | `frontend/src/app/dashboard/page.tsx` applied a 92%–95% opaque blue gradient `linear-gradient(135deg, rgba(37,99,255,0.92)...)` hiding the background image. | Medium | **FIXED** | Replaced opaque gradient in `dashboard/page.tsx` with a refined translucent dark/blue gradient overlay (`rgba(15, 23, 42, 0.72)` to `rgba(30, 58, 138, 0.62)`) ensuring `/hero1.png` is clearly visible with crisp text contrast. |
| **ISS-03** | I. AI GUARDIAN / H. GPS | AI Guardian queries ("What is the next petrol bunk?", "Find a pharmacy near me") did not search along active route or utilize verified places / POIs for fuel bunks and pharmacies. | `geminiToolRouter.ts` used synthetic fixed offset arrays without route-aware POI search (preferring fuel stations along active route geometry in travel direction) and lacked pharmacy category handlers. | Critical | **FIXED** | Updated `geminiToolRouter.ts` and `frontend/src/app/api/ai/route.ts` with route-aware POI matching for active corridor navigation, verified highway petrol bunks (Indian Oil, HP, BPCL), and complete pharmacy/chemist category handling. |
| **ISS-04** | I. AI GUARDIAN / G. MAPS | Pinned places discovered by AI Guardian must synchronize with Google Maps markers, auto-fit camera bounds, and provide bidirectional selection. | `GuardianMapSync.tsx` and `TravelAssistant.tsx` needed category marker color styling, camera bounds auto-fit on place updates, and direct map selection callbacks. | High | **FIXED** | Added `pharmacy` category colors to `CATEGORY_COLORS` in `GuardianMapSync.tsx`, bound auto-fit upon new place discovery, and linked interactive card clicking to map recentering. |
| **ISS-05** | H. GPS / G. GOOGLE MAPS | GPS speed & heading formatting displayed raw or null values when stationary or when browser geolocation returns `null`. | `navigationMath.ts` and `LiveNavigationOverlay.tsx` needed rigorous null/undefined checks outputting `"-- km/h"` and `"--"` to prevent misleading telemetry. | Medium | **FIXED** | Updated `formatSpeedKmh` and `formatHeading` in `navigationMath.ts` to output standard `"-- km/h"` and `"--"` when GPS speed or heading is null or unavailable. |
| **ISS-06** | J. EMERGENCY / H. GPS | Emergency hub location display must show human-readable reverse-geocoded address instead of only raw latitude/longitude coordinates. | `emergency/page.tsx` required reverse geocoding guarantee via `reverseGeocodeCoordinates` to display human-readable locality (e.g. "Kattankulathur, Tamil Nadu, India"). | High | **FIXED** | Verified `reverseGeocodeCoordinates` in `emergency/page.tsx` and emergency payload formatting; displays formatted address and links directly to Google Maps. |
| **ISS-07** | F. PERFORMANCE / G. MAPS | Google Maps JavaScript API script loader could encounter race conditions or duplicate script tag injection on rapid route transitions. | `googlePlaces.ts` required a robust singleton loading promise and authentication error handler for Google Maps JS API script injection. | Medium | **FIXED** | Verified singleton loader in `googlePlaces.ts` (`window.__googleMapsLoadingPromise`) with authentication error hook `window.gm_authFailure` and duplicate script tag deduplication. |
| **ISS-08** | E. UI/UX / K. MOBILE | Navigation HUD overlay on extra small mobile screens (320px–375px) required safe area padding and compact metric spacing to prevent clipping or overflow. | `LiveNavigationOverlay.tsx` required compact responsive grid/flex layout with `pb-safe` and mobile-friendly typography. | Medium | **FIXED** | Added `pb-safe`, responsive grid sizing (`grid-cols-4`), and compact typography in `LiveNavigationOverlay.tsx`. Tested at 390px mobile viewport without overflow. |
| **ISS-09** | E. UI/UX / L. DESKTOP | "Plan Journey" dashboard card and action modules needed perfect alignment, hover micro-interactions, and visual harmony. | `dashboard/page.tsx` modules grid needed streamlined icon containers and consistent typography. | Low | **FIXED** | Refined action cards in `dashboard/page.tsx` with smooth scale transitions, group hover effects, and crisp typography. |
| **ISS-10** | M. SECURITY / DOCS | Legacy documentation (`README.md`) contained obsolete references to Mapbox GL JS after full migration to pure Google Maps Platform. | Legacy markdown documentation had not been updated following the complete purge of Mapbox dependencies. | Low | **FIXED** | Cleaned up all obsolete Mapbox references in `README.md`, updating all diagrams, feature lists, tech stack tables, and environment specifications to pure Google Maps Platform. |

